<?xml version="1.0" encoding="utf-8"?>
<!--
   Acquisitions are encrypted to a hardware-bound, non-extractable device identity,
   so a backup or device transfer could only ever restore unreadable data. Exclude
   every domain (path="." = the whole domain) from both cloud backup and device-to-
   device transfer, including device-protected (Direct Boot) storage. Cloud/adb
   backup is additionally disabled via android:allowBackup="false", but that does
   not reliably stop device-to-device transfer on Android 12+.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
        <exclude domain="device_root" path="." />
        <exclude domain="device_file" path="." />
        <exclude domain="device_database" path="." />
        <exclude domain="device_sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
        <exclude domain="device_root" path="." />
        <exclude domain="device_file" path="." />
        <exclude domain="device_database" path="." />
        <exclude domain="device_sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
